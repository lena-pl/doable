<h1>Users</h1>

<% @users.each do |user| %>
    <h4><%= user.email %></h4>
    <p><%= link_to 'Show Profile', user %></p>
    <% if user.id === current_user.id %>
    	<p><%= link_to 'Edit', edit_user_path(user) %>
        <%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></p>
    <% end %>
<% end %>

<br>
